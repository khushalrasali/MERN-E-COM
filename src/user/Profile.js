import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="Profile" description="profile descrition">
      <h1>Profile page called</h1>
    </Base>
  );
};

export default Profile;
